{
  "name": "ParseBlog",
  "version": "0.0.1",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsconfig && tsc -p ."
  },
  "author": "Platypi",
  "license": "MIT",
  "dependencies": {
  }
}
